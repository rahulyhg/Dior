<?php
class siso_receipt_iostock_aftersaleChange implements siso_receipt_iostock_interface{

    /**
     *
     * 根据换货入库组织出入库单明细内容
     * @param int $delivery_id
     */
    function get_io_data($rp_id){
        
    }
}